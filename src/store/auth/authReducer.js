import initialState from './authInitialState'

/*const InitialState = {
  error: null,
  isFetching: false,
  firstName: '',
  lastName: '',
  email: '',
  accessToken: null,
  verifyEmailSuccessSent: false,
}*/

export default function authReducer(state = initialState, action) {
  switch (action.type) {
      default:
        return state
  }
}
